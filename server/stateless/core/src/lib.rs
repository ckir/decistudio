use anyhow::Result;

/// Core logic for stateless deployments (Lambda, Cloudflare Workers, etc.)
pub async fn handle_request() -> Result<()> {
    println!("decistudio stateless server core stub");
    Ok(())
}
